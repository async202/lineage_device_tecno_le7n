# Allow hal_secure_element to find MTK radio extension HW service
# (needed for NFC + eSE communication via MTK radio stack)
allow hal_secure_element_default hal_mtkradioex_hwservice:hwservice_manager find;
