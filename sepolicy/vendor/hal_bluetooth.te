# type hal_bluetooth_default, domain;
# type hal_bluetooth_default_exec, exec_type, file_type, vendor_file_type;

# hal_server_domain(hal_bluetooth_default, hal_bluetooth)

# init_daemon_domain(hal_bluetooth_default)

allow hal_bluetooth_client mtk_hal_bluetooth_hwservice:hwservice_manager find;

# neverallow hal_bluetooth_default sysfs:file { audit_access entrypoint execmod execute execute_no_trans mounton no_w_file_perms quotaon relabelto watch_mount watch_sb watch_with_perm };
