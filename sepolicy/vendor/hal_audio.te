# type hal_audio_default, domain;
# type hal_audio_default_exec, exec_type, file_type, vendor_file_type;

# hal_client_domain(hal_audio_default, hal_allocator)
# hal_server_domain(hal_audio_default, hal_audio)
# init_daemon_domain(hal_audio_default)

allow hal_audio_default audioserver:fifo_file write;

# neverallow hal_audio_default proc:file { audit_access entrypoint execmod execute execute_no_trans mounton no_w_file_perms quotaon relabelto watch_mount watch_sb watch_with_perm };
# neverallow hal_audio_default proc:lnk_file { audit_access execmod execute mounton no_w_file_perms quotaon relabelto watch_mount watch_sb watch_with_perm };
